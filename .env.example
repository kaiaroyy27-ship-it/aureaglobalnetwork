# Sanity CMS Configuration
# Get these values from your Sanity project settings at https://sanity.io

# Your Sanity project ID (find in sanity.json or project settings)
NEXT_PUBLIC_SANITY_PROJECT_ID=om0unvno

# Your Sanity dataset name (usually 'production')
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity API version (keep as is for this project)
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Optional: API token for server-side mutations (only needed if you implement mutations)
# SANITY_API_TOKEN=your_api_token_here
